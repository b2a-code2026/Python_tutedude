Basic_Maths.py takes two numbers as input from the user. Performs the basic mathematical operations - addition, subtraction, multiplication, division on these two numbers and displays the results of each operation on the screen.
Personalized_greeting.py takes a user's first name and last name as input. Concatenates the first name and last name into a full name and prints a personalized greeting message using the full name.

